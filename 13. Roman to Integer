class Solution:
    def romanToInt(self, s: str) -> int:
        values = {
            'I': 1, 'V': 5, 'X': 10, 'L': 50, 'C': 100, 'D': 500, 'M': 1000
        }
        total = 0
        n = len(s)
        for i in range(n) :
            curnt = values[s[i]]
            if i + 1 < n and curnt < values[s[i+1]] :
                total -= curnt
            else : 
                total += curnt
        return total

'''
Core Logic: for every letter in the string, if the letters values is greater than the next letter's value we add its actual value , if its less than the next letter's value we subtract it (example XL - X : 10 , L : 50 , 10 < 50 , so we do a -10, then L is last string, its value is 50 so we add 50 thus we get 40)

Edge cases:
1. Letter's value is less than the next letter's value
2. Last letter will not have a next letter to compare it to, should be handled and added by default

Soltuion try 1 :

class Solution:
    def romanToInt(self, s: str) -> int:
        values = {
            'I': 1, 'V': 5, 'X': 10, 'L': 50, 'C': 100, 'D': 500, 'M': 1000
        }
        total = 0
        n = len(s)
        for i in range(n) :
            if i != len(s) - 1 :
                crnt = values[s[i]]
                nxt =  values[s[i+1]]
                if crnt >= nxt :
                    total += crnt
                else :
                    total -= crnt
            else :
                total += values[s[i]]
 
        return total

Solution try 2:

class Solution:
    def romanToInt(self, s: str) -> int:
        values = {
            'I': 1, 'V': 5, 'X': 10, 'L': 50, 'C': 100, 'D': 500, 'M': 1000
        }
        total = 0
        n = len(s)
        for i in range(n) :
            curnt = values[s[i]]
            if i + 1 < n and curnt < values[s[i+1]] :
                total -= curnt
            else : 
                total += curnt
        return total
'''
